# Airtable personal access token — needs data.records:write scope on the beCamp base
AIRTABLE_TOKEN=

# Base ID starts with "app", find it at https://airtable.com/api
AIRTABLE_BASE_ID=

# Table name (or table ID starting with "tbl") for registrations
AIRTABLE_TABLE=Registrations
